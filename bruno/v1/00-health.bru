meta {
  name: 00 Health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
  body: none
  auth: none
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });

  test("body has ok=true", function () {
    const body = res.getBody();
    expect(body.ok).to.equal(true);
  });
}
